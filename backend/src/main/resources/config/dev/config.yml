spring:
  datasource:
    primary:
      driver-class-name:
      jdbc-url:
      username:
      password:
    secondary:
      driver-class-name:
      jdbc-url:
      username:
      password:
  autoconfigure:
    exclude:
      - org.springframework.session.autoconfigure.SessionAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

jasypt:
  encryptor:
    password: Ami2025!

i18n:
  message-file-path: "/sorc001/locale/"
  upload-cache-on-start: true

interceptor:
  authentication:
    exclude-paths: >-
      /api/auth/**,
      /error
