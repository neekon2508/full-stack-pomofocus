<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="backend.role.repository.RoleRepository">

    <!-- selectRoles --> 

    <select id="selectRoles" resultType="backend.role.model.RoleResponseVO">
        SELECT role_code
             , role_name
        FROM sys_roles R
        WHERE 1=1
        <if test='roleName != null and roleName != ""'>
            AND UPPER(R.role_name) LIKE CONCAT('%', UPPER(#{roleName, typeHandler=backend.common.handler.LikeEscapeTypeHandler}), '%')
        </if>
        ORDER BY role_code DESC
    </select>
</mapper>