import { Box } from "@mui/material";
import { useForm } from "react-hook-form";

function LoginForm() {
  //   const {
  //     register,
  //     handleSubmit,
  //     formState: { errors },
  //   } = useForm();
  //   return <Box component="form" onSubmit={handleSubmit(onSubmit)}></Box>;
}

export default LoginForm;
